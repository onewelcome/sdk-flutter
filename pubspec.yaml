name: onegini
description: A new flutter plugin project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  mockito: ^4.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.onegini.mobile.sdk.flutter
        pluginClass: OneginiPlugin
      ios:
        pluginClass: OneginiPlugin
